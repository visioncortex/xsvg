[package]
name = "xsvg-compile"
version = "0.0.0"
edition = "2021"
description = "The xsvg compiler core: xsvg/SVG in, plain-SVG-subset out. Platform-agnostic (no wasm/JS, no native font code)."
publish = false

[dependencies]
xsvg-core = { path = "../xsvg-core" }
roxmltree = "0.20"
